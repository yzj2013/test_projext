import { capitalizeString } from "./string_function"; // 不是  export default must use curly braces

const cap = capitalizeString("hello!");

console.log(cap);

import subtrace from "./index1"  // export default not curly braces